import React from "react";
import "./expired.scss";

const Expired: React.FC = () => {
    return (
        <div className="expired">
            <p>Expired</p>
        </div>
    );
};

export default Expired;